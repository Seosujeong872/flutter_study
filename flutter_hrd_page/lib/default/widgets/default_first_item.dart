import 'package:flutter/material.dart';

class DefaultFristItem extends StatelessWidget {
  const DefaultFristItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.yellow,);

  }
}
